{
  "name": "aquasense-server",
  "version": "1.0.0",
  "description": "Servidor Node.js para o AquaSense — lê o Arduino via serial e transmite ao navegador via WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serialport": "^12.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
